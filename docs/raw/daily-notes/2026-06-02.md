- For summary, totals only, no tax, no discounts
- Display BCA  + QRIS only as payment methods for now
- Add biz logo to invoice header
- Auto-save PDF to file on generate, display the saved file after
- Changing deployment directions, will deploy to Oracle Cloud, utilizing the free tier. This means the project's offline-only constraint can be omitted. May utilize useful React starter kit, TailwindCSS and probably shadcn/ui.
- The auto-saved PDF file should be saved in an Object Storage, Oracle provided, should be a generic S3-compatible API.
- Use Dockerfile and docker compose for deployment, utilize PHP-FPM.
- Add a new index page for listing invoices, use Storage facade, no SQL database needed.